# OPÇÃO 3: Se index.php está na raiz
[start]
cmd = 'php -S 0.0.0.0:$PORT'

# OPÇÃO 4: Forçar criação do diretório
# [phases.build]
# cmds = ['mkdir -p public && [ ! -f public/index.php ] && cp index.php public/ || true']
# [start]
# cmd = 'php -S 0.0.0.0:$PORT -t public'
